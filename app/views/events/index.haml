- update_content "#syndicate" do
  #syndicate
    %h3= t "syndicate.events.title"
    = render 'shared/items', :items => @items

= update_content "#say-something" do
  #say-something
    %h2= t ".title"
    .wrap
      = render 'shared/say_something_nav', :current => :event
      = render 'form', :event => Event.new

