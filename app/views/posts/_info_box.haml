= image_tag post.user.avatar.url, :width => 60, :height => 60
%h2 #{post.user.full_name}
%p.instruction #{post.body}
%p.age 
  #{time_ago_in_words(post.created_at, include_seconds = true)} ago
  - if post.replies.size > 0
    \- #{pluralize(post.replies.size, 'reply')}
  - else
    \- no replies yet

.replies
  - if post.replies.size > 0 # if there are replies
    %ol
      - post.replies.each do |reply|
        = render("replies/reply", :reply => reply)
  .new_reply
    = render 'replies/form', :post => post, :reply => Reply.new
  .clear