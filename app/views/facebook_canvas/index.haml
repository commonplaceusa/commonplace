= include_javascripts :application
= javascript_include_tag "http://connect.facebook.net/en_US/all.js#xfbml=1"
%script
  $(document).ready(function() {
  FB.init({
  appId : "#{($FacebookConfig || {})['app_id']}",
  status : true,
  cookie : true,
  xfbml  : true
  });
  s = FB.getSession()
  = "FB.api('/#{@request_id}?access_token=#{@application_token}', 'GET', {access_token: '#{@application_token}'}, function(r) { $('#join').attr('href','http://' + r.data + '.commonplaceusa.com/'); });"
  });
-# TODO: Change this from staging to production!
= link_to(image_tag("http://staging.commonplaceusa.com/images/join-commonplace.png"),"http://www.commonplaceusa.com/", :target => "_top", :id => "join")
#fb-root
