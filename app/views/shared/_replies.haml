%div{:id => "#{dom_id(item)}_replies", :class => ["replies",hide ? "hide": nil].join(" ")}
  = image_tag 'reply-arrow-top.png', :class => "reply-arrow"
  %ul
    - item.replies.each do |reply|
      = render 'replies/reply', :reply => reply
      
  = render 'replies/form', :reply => item.replies.new(:user => current_user)