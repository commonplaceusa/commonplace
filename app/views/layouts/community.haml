= render "layouts/top_html", :title => "CommonPlace: " + current_user.community.name, :stylesheets => 'application'

%body
  #wrap
    #top
    = render 'shared/header'
    #main
      = render 'shared/filters'
      #both_columns
        #left_col
          = yield
          .bottomshiv
        #right_col

  = render "layouts/bottom_html"
