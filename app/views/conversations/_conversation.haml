- content_tag(:li, :class => "conversation", "data-info" => render("conversations/message_list", :user => user) ) do
  // output as many avatars as possible
  - conversation.users.each do |user|
    = image_tag user.avatar.url, :width => 60, :height => 60
  %p #{conversation.subject}