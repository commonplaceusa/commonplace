require 'spec_helper'

describe GroupMembership do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    GroupMembership.create!(@valid_attributes)
  end
end
