- semantic_form_for [:management, @event] do |f|
  %h3.management_field Edit your event
  = f.inputs :name, :description
  = f.inputs :street_address, :zip_code, :for => :location
  = f.inputs :start_time, :end_time, :tag_list
  = f.buttons