- render :layout => 'shared/modal' do 
  
  %hr.staple
    
  %h1 Edit your profile fields
  %p.hint These profile fields will appear on your dedicated page. You can edit the subject and body of each field, as well as change the order.
  
  = render 'index_form'

  = button_to "New Profile Field", new_organization_profile_field_path(@organization), :method => :get, :class => "new"

  = button_to "Save order", order_organization_profile_fields_path(@organization), :method => :put, :class => "save"

  = button_to "Continue", new_organization_announcement_path(@organization), :method => :get, :class => "continue"