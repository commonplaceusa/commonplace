#!/bin/bash

cd ~/commonplace/ && git fetch && git reset --hard origin/master && bundle

script/remote_server/pull_environment
source ./.env

RAILS_ENV=remote_worker bundle exec rails runner KissmetricsDumpDownload.perform && bundle exec km_db_import km_dump
