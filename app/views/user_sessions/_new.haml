- semantic_form_for user_session, :url => user_session_path do |f|
  - f.inputs do
    = f.input :email
    = f.input :password, :hint => link_to("forgot password?", new_password_reset_path)
    = f.commit_button 'Log in'


