
- form_for reply do |f|
  = image_tag reply.user.avatar.url(:thumb), :class => "avatar"
  = f.hidden_field :repliable_id
  = f.hidden_field :repliable_type
  = f.text_area :body, "data-label" => "Reply here!", :rows => 1
  #submit_wrapper
    = f.submit "Reply"