
- form_for invite, :url => [:management,:invites], :html => {:class => "standard_form"} do |f|
  
  %h1 Invite #{ invite.invitee ? invite.invitee.name : invite.email } to join
  %fieldset
    %ol
      %li
        = f.label :body, "Message"
        = f.text_area :body, :rows => 4
        = f.hidden_field :inviter_id
        = f.hidden_field :inviter_type
        = f.hidden_field :invitee_id
      %li= f.submit "Send"
