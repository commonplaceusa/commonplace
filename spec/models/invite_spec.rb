require 'spec_helper'

describe Invite do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Invite.create!(@valid_attributes)
  end
end
