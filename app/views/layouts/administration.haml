!!! 5
%html
  %head
    %meta{ :content => "text/html;charset=utf-8", "http-equiv" => "Content-Type" }
    %meta{ :name => "google-site-verification", :content => "HntCS6GTLIAye5nM6FCNHgtcSUlb0-aF2jVbGtd8oyI" }
    %link{ :href => image_path('favicon.png'), :rel => "icon", :type => "image/png" }
    %title CommonPlace - Administration

    / [if IE]
      = javascript_include_tag "http://html5shiv.googlecode.com/svn/trunk/html5.js"    
    
    %script{ :type => 'text/javascript', :src => 'http://maps.google.com/maps/api/js?sensor=false' }
    
    = javascript_include_tag 'jquery-1.4.3', 'jquery-ui'
    = javascript_include_tag 'jquery/jquery.polygon-input', 'maps'

    = stylesheet_link_tag 'application'    
    %style{:type => "text/css"}
      = 'div.polygon-canvas { width: 600px; height: 300px; }'
      

  %body.application.administration
    #body_color
      #wrap
        - if current_user.admin?
          %header
            %h1 Administration
            %nav
              = link_to "log out", "/user_session", :method => :delete, :id => "logout"
              = render 'administration/navigation'
     
          #main
            = yield
        - else
          - form_for UserSession.new, :url => user_session_path do |f|
            .field.email
              %label= 'Email'.upcase
              = f.text_field :email, :size => 20
            .field.password
              %label= 'Password'.upcase
              = f.password_field :password, :size => 20
            .buttons.submit
              #submit_wrapper
                = f.submit "Log in".upcase
            %p.forgot_password= link_to "Forgot your password?", new_password_reset_path, 'data-remote' => true
