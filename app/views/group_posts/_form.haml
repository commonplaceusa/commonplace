

- semantic_form_for group_post, :html=>{'data-remote' => true} do |f|
  - f.inputs do
    = f.input :group, :collection => creation_groups_for(current_community)
    = f.input :subject
    = f.input :body
  - f.buttons do
    = f.commit_button :button_html => {:type => "image", :src => "/images/buttons/post-now-btn.png"}
