#both_columns
  #left_col
    %h3 Add a new profile field
    = render 'form', :profile_field => @profile_field

  #right_col
    %h3 Your Profile Fields
    %ul#wire
      - @profile_fields.sort{ |x,y| x.position <=> y.position }.each do |field|
        %li.profile_field
          %h4= field.subject
          %p= field.body
