require 'spec_helper'

describe Attendances do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Attendances.create!(@valid_attributes)
  end
end
